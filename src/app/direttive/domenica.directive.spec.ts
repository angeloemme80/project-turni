import { DomenicaDirective } from './domenica.directive';

describe('DomenicaDirective', () => {
  it('should create an instance', () => {
    const directive = new DomenicaDirective();
    expect(directive).toBeTruthy();
  });
});
